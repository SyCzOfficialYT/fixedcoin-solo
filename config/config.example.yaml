# Wird beim Container-Start automatisch als config.yaml erzeugt.
# Optional: FIX_RPCPASS / eigene payout_address per Env oder nach erstem Boot überschreiben.
rpc:
  host: "127.0.0.1"
  port: 24761
  user: "fixrpc"
  password: "FixedcoinSoloAutoRpc_ChangeMeIfPublic"

pool:
  payout_address: "fix1CHANGE_ME_GETNEWADDRESS"
  start_difficulty: 10000
  fixed_difficulty: 13354
  min_difficulty: 1000
  job_interval: 30
  stratum_host: "0.0.0.0"
  stratum_port: 3333

monitor:
  host: "0.0.0.0"
  port: 5050
